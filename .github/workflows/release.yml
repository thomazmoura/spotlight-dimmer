name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g., v1.0.0)'
        required: true
        type: string

env:
  CARGO_INCREMENTAL: 0
  RUST_BACKTRACE: short

jobs:
  changelog:
    runs-on: ubuntu-latest
    outputs:
      release-notes: ${{ steps.changelog.outputs.release-notes }}
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get version from tag
        id: version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ inputs.tag }}"
          else
            VERSION="${{ github.ref_name }}"
          fi
          echo "version=${VERSION#v}" >> $GITHUB_OUTPUT
          echo "tag=${VERSION}" >> $GITHUB_OUTPUT

      - name: Extract changelog
        id: changelog
        run: |
          # Extract unreleased section from CHANGELOG.md using external script
          python3 .github/scripts/extract_changelog.py

  build-and-release:
    needs: changelog
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-gnu
          components: rustfmt, clippy

      - name: Install MinGW
        run: |
          sudo apt-get update
          sudo apt-get install -y mingw-w64

      - name: Rust cache
        uses: swatinem/rust-cache@v2

      - name: Run tests
        run: cargo test --lib --verbose --target x86_64-pc-windows-gnu

      - name: Run clippy
        run: cargo clippy --all-targets --all-features --target x86_64-pc-windows-gnu -- -W clippy::all -A dead_code

      - name: Build binaries
        run: |
          cargo build --release --target x86_64-pc-windows-gnu --bin spotlight-dimmer --bin spotlight-dimmer-config

      - name: Create ZIP archive
        run: |
          version="${{ needs.changelog.outputs.version }}"
          zipName="spotlight-dimmer-v${version}-windows-x86_64.zip"
          zip $zipName target/x86_64-pc-windows-gnu/release/spotlight-dimmer.exe target/x86_64-pc-windows-gnu/release/spotlight-dimmer-config.exe spotlight-dimmer-icon.ico spotlight-dimmer-icon-paused.ico
          echo "ASSET_PATH=$zipName" >> $GITHUB_ENV

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ needs.changelog.outputs.version && format('v{0}', needs.changelog.outputs.version) || github.ref_name }}
          name: 'Spotlight Dimmer v${{ needs.changelog.outputs.version || github.ref_name }}'
          body: |
            ${{ needs.changelog.outputs.release-notes }}

            ## Installation

            ### Direct Download (Recommended)
            Download the ZIP file below and extract all files:
            - `spotlight-dimmer.exe` - Main application (~561 KB)
            - `spotlight-dimmer-config.exe` - Configuration tool (~627 KB)
            - `spotlight-dimmer-icon.ico` - System tray icon (required)
            - `spotlight-dimmer-icon-paused.ico` - Paused state icon (required)

            **Important**: Keep the icon files in the same directory as the executables for the system tray to work correctly.

            ### Via Cargo
            ```bash
            cargo install --git https://github.com/thomazmoura/spotlight-dimmer --branch main spotlight-dimmer
            ```

            ## What's Spotlight Dimmer?

            Spotlight Dimmer dims inactive displays to help you focus on the active one. Perfect for multi-monitor setups!

            - 🎯 **Ultra-lightweight**: Only ~7.6 MB RAM usage
            - 🔄 **Real-time Monitoring**: Instantly detects window focus changes (100ms polling)
            - 👻 **Click-through Overlays**: Dimming overlays don't interfere with your workflow
            - ⚡ **Native Windows API**: No browser engine overhead, instant startup
            - ⚙️ **Auto-reload Config**: Changes detected within 2 seconds, no restart needed

            ---

            🤖 *Generated with [Claude Code](https://claude.com/claude-code)*
          draft: false
          prerelease: false
          files: ${{ env.ASSET_PATH }}

  publish-cargo:
    needs: [changelog, build-and-release]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Publish to crates.io
        continue-on-error: true
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_TOKEN }}
        run: |
          if [ -z "$CARGO_REGISTRY_TOKEN" ]; then
            echo "⚠️  CARGO_TOKEN secret not set - skipping crates.io publish"
            echo "ℹ️  Add CARGO_TOKEN secret to enable automated crates.io publishing"
            exit 0
          fi
          cargo publish --allow-dirty

  publish-npm:
    needs: [changelog, build-and-release]
    if: always()
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-gnu

      - name: Install MinGW
        run: |
          sudo apt-get update
          sudo apt-get install -y mingw-w64

      - name: Build binaries for npm
        run: |
          cargo build --release --target x86_64-pc-windows-gnu --bin spotlight-dimmer --bin spotlight-dimmer-config

      - name: Prepare npm package
        run: |
          # Copy executables to bin directory
          cp target/x86_64-pc-windows-gnu/release/spotlight-dimmer.exe bin/
          cp target/x86_64-pc-windows-gnu/release/spotlight-dimmer-config.exe bin/

          # Copy icon files to bin directory
          cp spotlight-dimmer-icon.ico bin/
          cp spotlight-dimmer-icon-paused.ico bin/

          echo "Package contents ready for npm publish"

      - name: Publish to npm
        continue-on-error: true
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          if [ -z "$NODE_AUTH_TOKEN" ]; then
            echo "⚠️  NPM_TOKEN secret not set - skipping npm publish"
            echo "ℹ️  Add NPM_TOKEN secret to enable automated npm publishing"
            exit 0
          fi
          npm publish --provenance --access public

  update-changelog:
    needs: [changelog, build-and-release]
    runs-on: ubuntu-latest
    if: success()
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
          ref: main

      - name: Update changelog
        run: |
          # Update CHANGELOG.md to move unreleased content to new version using external script
          python3 .github/scripts/update_changelog.py "${{ needs.changelog.outputs.version }}"

      - name: Commit changelog update
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add CHANGELOG.md
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update CHANGELOG.md for release v${{ needs.changelog.outputs.version }}"
            git push
          fi
