{
  "name": "spotlight-dimmer",
  "version": "0.1.0",
  "description": "A cross-platform application that dims inactive displays to highlight the active one",
  "main": "index.html",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "serve": "python -m http.server 1420 --directory dist",
    "serve-node": "npx http-server dist -p 1420 --cors"
  },
  "keywords": ["tauri", "rust", "display", "focus"],
  "author": "Spotlight Dimmer Team",
  "license": "MIT",
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}